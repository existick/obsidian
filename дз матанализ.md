# Формула Муавра

## Теоретическая база (подробно, с нуля)

**Что такое комплексное число и зачем нужна тригонометрическая форма.**

Комплексное число $z=a+bi$ можно изобразить точкой на плоскости с координатами $(a,b)$: $a$ — по горизонтальной ("действительной") оси, $b$ — по вертикальной ("мнимой"). Эту же точку можно описать не координатами $(a,b)$, а по-другому — расстоянием от начала координат и углом поворота. Это и есть тригонометрическая форма:  
$$  
z = r(\cos\varphi+i\sin\varphi)  
$$  
где:

- $r=|z|=\sqrt{a^2+b^2}$ — **модуль**, то есть расстояние от точки $z$ до начала координат (по теореме Пифагора: катеты $a$ и $b$, гипотенуза — искомое расстояние);
- $\varphi=\arg z$ — **аргумент**, угол между положительным направлением действительной оси и отрезком, соединяющим начало координат с точкой $z$ (отсчитывается против часовой стрелки, если угол считаем положительным).

Связь координат $(a,b)$ и пары $(r,\varphi)$ такая же, как между декартовыми и полярными координатами точки:  
$$  
a = r\cos\varphi,\qquad b=r\sin\varphi  
$$

**Как находить $\varphi$ практически.** Чтобы не ошибиться со знаком угла, удобна такая процедура:

1. Вычислить **базовый (острый) угол** $\alpha=\arctan\dfrac{|b|}{|a|}$ — это угол, который отрезок образует с ближайшей горизонтальной осью, если "забыть" про знаки $a$ и $b$. Он всегда лежит в промежутке $(0,\pi/2)$.
2. Определить четверть, в которой лежит точка $(a,b)$, по знакам $a$ и $b$, и по этой четверти скорректировать знак и "сдвиг" угла:

|Четверть|Знаки $(a,b)$|$\varphi$ через базовый угол $\alpha$|
|---|---|---|
|I|$(+,+)$|$\varphi=\alpha$|
|II|$(-,+)$|$\varphi=\pi-\alpha$|
|III|$(-,-)$|$\varphi=-(\pi-\alpha)$ (эквивалентно $\pi+\alpha$)|
|IV|$(+,-)$|$\varphi=-\alpha$|

3. Обязательно **проверять себя**: подставить найденные $r$ и $\varphi$ обратно в $a=r\cos\varphi$, $b=r\sin\varphi$ и убедиться, что получаются исходные числа.

**Формула Муавра — что это и откуда берётся.**

Когда мы умножаем два комплексных числа в тригонометрической форме, их модули перемножаются, а аргументы складываются:  
$$  
z_1 z_2 = r_1 r_2\big(\cos(\varphi_1+\varphi_2)+i\sin(\varphi_1+\varphi_2)\big)  
$$  
Это следует из тригонометрических формул синуса и косинуса суммы — при прямом перемножении $r_1(\cos\varphi_1+i\sin\varphi_1)\cdot r_2(\cos\varphi_2+i\sin\varphi_2)$ и раскрытии скобок с учётом $i^2=-1$ действительная часть результата в точности совпадает с $\cos(\varphi_1+\varphi_2)$, а мнимая — с $\sin(\varphi_1+\varphi_2)$ (это стандартный факт тригонометрии, не будем его передоказывать).

Если применить это правило $n$ раз подряд к одному и тому же числу $z=r(\cos\varphi+i\sin\varphi)$ (то есть посчитать $z\cdot z\cdot\ldots\cdot z$), модуль умножится сам на себя $n$ раз (то есть станет $r^n$), а аргумент сложится сам с собой $n$ раз (то есть станет $n\varphi$). Отсюда:  
$$  
\boxed{z^n = r^n(\cos n\varphi+i\sin n\varphi)}\qquad\text{(формула Муавра)}  
$$

Аналогично для деления (это то же умножение на число, обратное второму множителю) модули делятся, а аргументы вычитаются:  
$$  
\frac{z_1}{z_2}=\frac{r_1}{r_2}\big(\cos(\varphi_1-\varphi_2)+i\sin(\varphi_1-\varphi_2)\big)  
$$

**Почему угол нужно "приводить" (брать по модулю $2\pi$).**

Функции $\cos$ и $\sin$ периодические с периодом $2\pi$ — это значит, что $\cos(\varphi+2\pi)=\cos\varphi$ и то же для синуса (поворот на полный круг возвращает точку в то же положение). Поэтому если после применения формулы Муавра получился угол вида $1000\pi$ или $-37\pi/2$, его не нужно вычислять "напрямую" — нужно вычесть или прибавить столько полных оборотов $2\pi$, сколько нужно, чтобы попасть в table-значение (например, в промежуток $[0,2\pi)$ или $(-\pi,\pi]$), а затем уже брать косинус и синус от простого табличного угла.

С этим аппаратом решаем все 10 примеров.

---

## 1. $z=(1-i)^{20}$

**Текст задания:** $z=(1-i)^{20}$

**Решения:**

**Шаг 1. Переводим число $1-i$ в тригонометрическую форму.**

Действительная часть $a=1$, мнимая часть $b=-1$ (коэффициент при $i$).

Модуль по теореме Пифагора:  
$$  
r=\sqrt{a^2+b^2}=\sqrt{1^2+(-1)^2}=\sqrt{1+1}=\sqrt2  
$$

**Шаг 2. Определяем аргумент.**

Знаки: $a=1>0$, $b=-1<0$ — точка $(1,-1)$ лежит в **IV четверти** (правая нижняя часть плоскости).

Базовый угол:  
$$  
\alpha=\arctan\frac{|b|}{|a|}=\arctan\frac{1}{1}=\arctan1=\frac{\pi}{4}  
$$  
(это табличное значение: угол, тангенс которого равен 1, равен 45° = $\pi/4$).

Для IV четверти аргумент отрицательный и равен базовому углу со знаком минус:  
$$  
\varphi=-\frac{\pi}{4}  
$$

**Шаг 3. Проверка.**

$$  
r\cos\varphi=\sqrt2\cdot\cos\left(-\frac{\pi}{4}\right)=\sqrt2\cdot\frac{\sqrt2}{2}=\frac{2}{2}=1=a\ \checkmark  
$$  
$$  
r\sin\varphi=\sqrt2\cdot\sin\left(-\frac{\pi}{4}\right)=\sqrt2\cdot\left(-\frac{\sqrt2}{2}\right)=-1=b\ \checkmark  
$$

Значит, $1-i=\sqrt2\left(\cos\left(-\dfrac{\pi}{4}\right)+i\sin\left(-\dfrac{\pi}{4}\right)\right)$ — записано верно.

**Шаг 4. Применяем формулу Муавра для $n=20$.**

По формуле $z^n=r^n(\cos n\varphi+i\sin n\varphi)$ отдельно считаем новый модуль и новый угол.

_Новый модуль:_  
$$  
r^{20}=(\sqrt2)^{20}  
$$  
Здесь удобно записать $\sqrt2=2^{1/2}$, тогда $(\sqrt2)^{20}=\left(2^{1/2}\right)^{20}=2^{20/2}=2^{10}$.  
$$  
2^{10}=1024  
$$

_Новый угол:_  
$$  
n\varphi=20\cdot\left(-\frac{\pi}{4}\right)=-\frac{20\pi}{4}=-5\pi  
$$

**Шаг 5. Приводим угол $-5\pi$ к табличному виду.**

Прибавляем полные обороты $2\pi$, пока угол не попадёт в удобный диапазон. Прибавим $2\pi$ трижды (то есть $6\pi$):  
$$  
-5\pi+6\pi=\pi  
$$  
Значит, угол $-5\pi$ "смотрит" в ту же сторону, что и угол $\pi$ (разница — целое число полных оборотов, а после целого числа оборотов стрелка часов указывает туда же).

**Шаг 6. Вычисляем косинус и синус табличного угла $\pi$.**

$$  
\cos\pi=-1,\qquad\sin\pi=0  
$$  
(угол $\pi$ = 180°, это направление строго влево по действительной оси).

**Шаг 7. Собираем ответ.**

$$  
z=1024\cdot(\cos\pi+i\sin\pi)=1024\cdot(-1+0\cdot i)=-1024  
$$

**Ответ:** $z=-1024$

---

## 2. $z=(\sqrt3+i)^{15}$

**Текст задания:** $z=(\sqrt3+i)^{15}$

**Решения:**

**Шаг 1. Модуль числа $\sqrt3+i$.**

$a=\sqrt3$, $b=1$.  
$$  
r=\sqrt{(\sqrt3)^2+1^2}=\sqrt{3+1}=\sqrt4=2  
$$

**Шаг 2. Аргумент.**

$a=\sqrt3>0$, $b=1>0$ — точка в **I четверти** (обе координаты положительны, самый простой случай — аргумент равен базовому углу без каких-либо поправок).

$$  
\varphi=\arctan\frac{b}{a}=\arctan\frac{1}{\sqrt3}  
$$  
Это табличное значение: $\arctan\dfrac{1}{\sqrt3}=\dfrac{\pi}{6}$ (30°).

**Шаг 3. Проверка.**  
$$  
r\cos\varphi=2\cos\frac{\pi}{6}=2\cdot\frac{\sqrt3}{2}=\sqrt3=a\ \checkmark  
$$  
$$  
r\sin\varphi=2\sin\frac{\pi}{6}=2\cdot\frac12=1=b\ \checkmark  
$$

**Шаг 4. Формула Муавра, $n=15$.**

_Модуль:_ $r^{15}=2^{15}$.

Считаем: $2^{10}=1024$, $2^{15}=2^{10}\cdot2^5=1024\cdot32=32768$.

_Угол:_  
$$  
15\varphi=15\cdot\frac{\pi}{6}=\frac{15\pi}{6}=\frac{5\pi}{2}  
$$  
(сократили дробь $15/6=5/2$).

**Шаг 5. Приводим угол.**

$$  
\frac{5\pi}{2}-2\pi=\frac{5\pi}{2}-\frac{4\pi}{2}=\frac{\pi}{2}  
$$  
Угол $\dfrac{5\pi}{2}$ эквивалентен табличному $\dfrac{\pi}{2}$ (90°).

**Шаг 6. Косинус и синус.**  
$$  
\cos\frac{\pi}{2}=0,\qquad\sin\frac{\pi}{2}=1  
$$

**Шаг 7. Ответ.**  
$$  
z=32768\cdot(0+1\cdot i)=32768i  
$$

**Ответ:** $z=32768i$

---

## 3. $z=(-2+2i\sqrt3)^{9}$

**Текст задания:** $z=(-2+2i\sqrt3)^{9}$

**Решения:**

**Шаг 1. Модуль.**

$a=-2$, $b=2\sqrt3$.  
$$  
r=\sqrt{(-2)^2+(2\sqrt3)^2}=\sqrt{4+4\cdot3}=\sqrt{4+12}=\sqrt{16}=4  
$$

**Шаг 2. Аргумент.**

$a=-2<0$, $b=2\sqrt3>0$ — точка в **II четверти** (левая верхняя часть).

Базовый угол:  
$$  
\alpha=\arctan\frac{|b|}{|a|}=\arctan\frac{2\sqrt3}{2}=\arctan\sqrt3=\frac{\pi}{3}  
$$  
(табличное значение — тангенс $60°$ равен $\sqrt3$).

Для II четверти: аргумент — это "недокрут" до $180°$, то есть  
$$  
\varphi=\pi-\alpha=\pi-\frac{\pi}{3}=\frac{3\pi}{3}-\frac{\pi}{3}=\frac{2\pi}{3}  
$$

**Шаг 3. Проверка.**  
$$  
r\cos\varphi=4\cos\frac{2\pi}{3}=4\cdot\left(-\frac12\right)=-2=a\ \checkmark  
$$  
$$  
r\sin\varphi=4\sin\frac{2\pi}{3}=4\cdot\frac{\sqrt3}{2}=2\sqrt3=b\ \checkmark  
$$

**Шаг 4. Формула Муавра, $n=9$.**

_Модуль:_ $r^9=4^9$.  
$$  
4^9=(2^2)^9=2^{18}  
$$  
Считаем: $2^{10}=1024$, $2^{18}=2^{10}\cdot2^8=1024\cdot256=262144$.

_Угол:_  
$$  
9\varphi=9\cdot\frac{2\pi}{3}=\frac{18\pi}{3}=6\pi  
$$

**Шаг 5. Приводим угол.**

$6\pi$ — это ровно 3 полных оборота ($6\pi=3\cdot2\pi$), поэтому угол эквивалентен $0$.

**Шаг 6. Косинус и синус.**  
$$  
\cos0=1,\qquad\sin0=0  
$$

**Шаг 7. Ответ.**  
$$  
z=262144\cdot(1+0\cdot i)=262144  
$$

**Ответ:** $z=262144$

---

## 4. $z=\left(-\dfrac12-i\dfrac{\sqrt3}{2}\right)^{45}$

**Текст задания:** $z=\left(-\dfrac12-i\dfrac{\sqrt3}{2}\right)^{45}$

**Решения:**

**Шаг 1. Модуль.**

$a=-\dfrac12$, $b=-\dfrac{\sqrt3}{2}$.  
$$  
r=\sqrt{\left(-\frac12\right)^2+\left(-\frac{\sqrt3}{2}\right)^2}=\sqrt{\frac14+\frac34}=\sqrt{\frac44}=\sqrt1=1  
$$

Модуль равен 1 — число лежит на единичной окружности.

**Шаг 2. Аргумент.**

$a<0$, $b<0$ — **III четверть** (левая нижняя часть).

Базовый угол:  
$$  
\alpha=\arctan\frac{|b|}{|a|}=\arctan\frac{\sqrt3/2}{1/2}=\arctan\sqrt3=\frac{\pi}{3}  
$$

Для III четверти удобно брать аргумент в отрицательном виде (по часовой стрелке от положительной полуоси):  
$$  
\varphi=-(\pi-\alpha)=-\left(\pi-\frac{\pi}{3}\right)=-\frac{2\pi}{3}  
$$

**Шаг 3. Проверка.**  
$$  
\cos\left(-\frac{2\pi}{3}\right)=\cos\frac{2\pi}{3}=-\frac12=a\ \checkmark  
$$  
$$  
\sin\left(-\frac{2\pi}{3}\right)=-\sin\frac{2\pi}{3}=-\frac{\sqrt3}{2}=b\ \checkmark  
$$  
(здесь использовано: косинус — чётная функция, $\cos(-x)=\cos x$; синус — нечётная, $\sin(-x)=-\sin x$).

**Шаг 4. Формула Муавра, $n=45$.**

_Модуль:_ $r^{45}=1^{45}=1$ (единица в любой степени — единица).

_Угол:_  
$$  
45\varphi=45\cdot\left(-\frac{2\pi}{3}\right)=-\frac{90\pi}{3}=-30\pi  
$$

**Шаг 5. Приводим угол.**

$-30\pi$ — это $-15$ полных оборотов ($-30\pi=-15\cdot2\pi$), то есть целое число оборотов. Значит, угол эквивалентен $0$.

**Шаг 6. Косинус и синус.**  
$$  
\cos0=1,\qquad\sin0=0  
$$

**Шаг 7. Ответ.**  
$$  
z=1\cdot(1+0\cdot i)=1  
$$

**Ответ:** $z=1$

---

## 5. $z=\left(\dfrac{1+i}{\sqrt3-i}\right)^{12}$

**Текст задания:** $z=\left(\dfrac{1+i}{\sqrt3-i}\right)^{12}$

**Решения:**

Здесь под степенью стоит не одно число, а дробь. Прежде чем применять формулу Муавра, нужно записать в тригонометрической форме отдельно числитель и знаменатель, а затем воспользоваться правилом деления: модули делятся, аргументы вычитаются.

**Шаг 1. Числитель $1+i$.**

$a=1$, $b=1$ — **I четверть**.  
$$  
r_1=\sqrt{1^2+1^2}=\sqrt2,\qquad \varphi_1=\arctan\frac{1}{1}=\arctan1=\frac{\pi}{4}  
$$

**Шаг 2. Знаменатель $\sqrt3-i$.**

$a=\sqrt3$, $b=-1$ — **IV четверть**.  
$$  
r_2=\sqrt{(\sqrt3)^2+(-1)^2}=\sqrt{3+1}=2  
$$  
Базовый угол: $\arctan\dfrac{1}{\sqrt3}=\dfrac{\pi}{6}$, для IV четверти:  
$$  
\varphi_2=-\frac{\pi}{6}  
$$

**Шаг 3. Делим числитель на знаменатель.**

Модуль частного — это модуль числителя, делённый на модуль знаменателя:  
$$  
r=\frac{r_1}{r_2}=\frac{\sqrt2}{2}  
$$  
Аргумент частного — это аргумент числителя минус аргумент знаменателя (при делении, в отличие от умножения, углы **вычитаются**, а не складываются):  
$$  
\varphi=\varphi_1-\varphi_2=\frac{\pi}{4}-\left(-\frac{\pi}{6}\right)=\frac{\pi}{4}+\frac{\pi}{6}  
$$

Приводим к общему знаменателю (НОК чисел 4 и 6 равен 12):  
$$  
\frac{\pi}{4}=\frac{3\pi}{12},\qquad\frac{\pi}{6}=\frac{2\pi}{12}  
$$  
$$  
\varphi=\frac{3\pi}{12}+\frac{2\pi}{12}=\frac{5\pi}{12}  
$$

Итак, $\dfrac{1+i}{\sqrt3-i}=\dfrac{\sqrt2}{2}\left(\cos\dfrac{5\pi}{12}+i\sin\dfrac{5\pi}{12}\right)$.

**Шаг 4. Применяем формулу Муавра к этому частному, $n=12$.**

_Модуль:_  
$$  
r^{12}=\left(\frac{\sqrt2}{2}\right)^{12}  
$$  
Упростим основание: $\dfrac{\sqrt2}{2}=\dfrac{2^{1/2}}{2}=2^{1/2-1}=2^{-1/2}$.  
$$  
\left(2^{-1/2}\right)^{12}=2^{-1/2\cdot12}=2^{-6}=\frac{1}{2^6}=\frac{1}{64}  
$$

_Угол:_  
$$  
12\varphi=12\cdot\frac{5\pi}{12}=5\pi  
$$

**Шаг 5. Приводим угол.**

$$  
5\pi-4\pi=\pi  
$$  
($4\pi$ — это два полных оборота, вычитаем их).

**Шаг 6. Косинус и синус.**  
$$  
\cos\pi=-1,\qquad\sin\pi=0  
$$

**Шаг 7. Ответ.**  
$$  
z=\frac{1}{64}\cdot(-1+0\cdot i)=-\frac{1}{64}  
$$

**Ответ:** $z=-\dfrac{1}{64}$

---

## 6. $z=\left(\dfrac{-4i}{1-i\sqrt3}\right)^{18}$

**Текст задания:** $z=\left(\dfrac{-4i}{1-i\sqrt3}\right)^{18}$

**Решения:**

**Шаг 1. Числитель $-4i$.**

Это число вида $0-4i$, то есть $a=0$, $b=-4$ — чисто мнимое число, лежащее строго на отрицательной части мнимой оси (то есть "между" III и IV четвертями, ровно на границе).  
$$  
r_1=\sqrt{0^2+(-4)^2}=\sqrt{16}=4  
$$  
Точка на отрицательной мнимой оси соответствует углу $-\dfrac{\pi}{2}$ (90° по часовой стрелке от положительной действительной оси):  
$$  
\varphi_1=-\frac{\pi}{2}  
$$  
Проверка: $r_1\cos\varphi_1=4\cos(-\pi/2)=4\cdot0=0=a\ \checkmark$; $r_1\sin\varphi_1=4\sin(-\pi/2)=4\cdot(-1)=-4=b\ \checkmark$.

**Шаг 2. Знаменатель $1-i\sqrt3$.**

$a=1$, $b=-\sqrt3$ — **IV четверть**.  
$$  
r_2=\sqrt{1^2+(\sqrt3)^2}=\sqrt{1+3}=2  
$$  
Базовый угол: $\arctan\dfrac{\sqrt3}{1}=\arctan\sqrt3=\dfrac{\pi}{3}$. Для IV четверти:  
$$  
\varphi_2=-\frac{\pi}{3}  
$$

**Шаг 3. Делим.**

$$  
r=\frac{r_1}{r_2}=\frac{4}{2}=2  
$$  
$$  
\varphi=\varphi_1-\varphi_2=-\frac{\pi}{2}-\left(-\frac{\pi}{3}\right)=-\frac{\pi}{2}+\frac{\pi}{3}  
$$  
Общий знаменатель 6: $-\dfrac{3\pi}{6}+\dfrac{2\pi}{6}=-\dfrac{\pi}{6}$.

**Шаг 4. Формула Муавра, $n=18$.**

_Модуль:_  
$$  
r^{18}=2^{18}  
$$  
Считаем: $2^{10}=1024$, $2^{18}=2^{10}\cdot2^8=1024\cdot256=262144$.

_Угол:_  
$$  
18\varphi=18\cdot\left(-\frac{\pi}{6}\right)=-\frac{18\pi}{6}=-3\pi  
$$

**Шаг 5. Приводим угол.**

$$  
-3\pi+4\pi=\pi  
$$  
(прибавили $4\pi$ = два полных оборота).

**Шаг 6. Косинус и синус.**  
$$  
\cos\pi=-1,\qquad\sin\pi=0  
$$

**Шаг 7. Ответ.**  
$$  
z=262144\cdot(-1+0\cdot i)=-262144  
$$

**Ответ:** $z=-262144$

---

## 7. $z=\left(\dfrac{\sqrt2-i\sqrt2}{-\sqrt3-i}\right)^{30}$

**Текст задания:** $z=\left(\dfrac{\sqrt2-i\sqrt2}{-\sqrt3-i}\right)^{30}$

**Решения:**

**Шаг 1. Числитель $\sqrt2-i\sqrt2$.**

$a=\sqrt2$, $b=-\sqrt2$ — **IV четверть** (действительная часть положительна, мнимая отрицательна).  
$$  
r_1=\sqrt{(\sqrt2)^2+(\sqrt2)^2}=\sqrt{2+2}=\sqrt4=2  
$$  
Базовый угол: $\arctan\dfrac{\sqrt2}{\sqrt2}=\arctan1=\dfrac{\pi}{4}$. Для IV четверти:  
$$  
\varphi_1=-\frac{\pi}{4}  
$$

**Шаг 2. Знаменатель $-\sqrt3-i$.**

$a=-\sqrt3$, $b=-1$ — **III четверть** (обе координаты отрицательны).  
$$  
r_2=\sqrt{(\sqrt3)^2+1^2}=\sqrt{3+1}=2  
$$  
Базовый угол: $\arctan\dfrac{1}{\sqrt3}=\dfrac{\pi}{6}$. Для III четверти:  
$$  
\varphi_2=-\left(\pi-\frac{\pi}{6}\right)=-\frac{5\pi}{6}  
$$

Проверка: $\cos(-5\pi/6)=-\cos(\pi/6)=-\dfrac{\sqrt3}{2}$, тогда $r_2\cos\varphi_2=2\cdot\left(-\dfrac{\sqrt3}{2}\right)=-\sqrt3=a\ \checkmark$. $\sin(-5\pi/6)=-\sin(\pi/6)=-\dfrac12$, тогда $r_2\sin\varphi_2=2\cdot\left(-\dfrac12\right)=-1=b\ \checkmark$.

**Шаг 3. Делим.**

$$  
r=\frac{2}{2}=1  
$$  
$$  
\varphi=\varphi_1-\varphi_2=-\frac{\pi}{4}-\left(-\frac{5\pi}{6}\right)=-\frac{\pi}{4}+\frac{5\pi}{6}  
$$  
Общий знаменатель 12:  
$$  
-\frac{3\pi}{12}+\frac{10\pi}{12}=\frac{7\pi}{12}  
$$

**Шаг 4. Формула Муавра, $n=30$.**

_Модуль:_ $r^{30}=1^{30}=1$.

_Угол:_  
$$  
30\varphi=30\cdot\frac{7\pi}{12}=\frac{210\pi}{12}=\frac{35\pi}{2}  
$$  
(сократили $210/12=35/2$).

**Шаг 5. Приводим угол.**

Нужно найти, сколько раз вычесть $2\pi=\dfrac{4\pi}{2}$ из $\dfrac{35\pi}{2}$, чтобы попасть в диапазон $[0,2\pi)$, то есть в диапазон числителей $[0,4\pi)$ при знаменателе 2.

$$  
\frac{35\pi}{2}-\frac{32\pi}{2}=\frac{3\pi}{2}  
$$  
(вычли $\dfrac{32\pi}{2}=16\pi$, то есть 8 полных оборотов).

**Шаг 6. Косинус и синус угла $\dfrac{3\pi}{2}$.**

Угол $\dfrac{3\pi}{2}$ (270°) указывает строго вниз по мнимой оси:  
$$  
\cos\frac{3\pi}{2}=0,\qquad\sin\frac{3\pi}{2}=-1  
$$

**Шаг 7. Ответ.**  
$$  
z=1\cdot(0+(-1)\cdot i)=-i  
$$

**Ответ:** $z=-i$

---

## 8. $z=\big((1+i\sqrt3)(2-2i)\big)^{8}$

**Текст задания:** $z=\big((1+i\sqrt3)(2-2i)\big)^{8}$

**Решения:**

Внутри скобки стоит **произведение** двух чисел, а не дробь. При умножении модули перемножаются, аргументы складываются.

**Шаг 1. Первый множитель $1+i\sqrt3$.**

$a=1$, $b=\sqrt3$ — **I четверть**.  
$$  
r_1=\sqrt{1^2+(\sqrt3)^2}=\sqrt{1+3}=2  
$$  
$$  
\varphi_1=\arctan\frac{\sqrt3}{1}=\arctan\sqrt3=\frac{\pi}{3}  
$$

**Шаг 2. Второй множитель $2-2i$.**

$a=2$, $b=-2$ — **IV четверть**.  
$$  
r_2=\sqrt{2^2+(-2)^2}=\sqrt{4+4}=\sqrt8=2\sqrt2  
$$  
Базовый угол: $\arctan\dfrac{2}{2}=\arctan1=\dfrac{\pi}{4}$. Для IV четверти:  
$$  
\varphi_2=-\frac{\pi}{4}  
$$

**Шаг 3. Перемножаем (модули — умножаем, аргументы — складываем).**

$$  
r=r_1r_2=2\cdot2\sqrt2=4\sqrt2  
$$  
$$  
\varphi=\varphi_1+\varphi_2=\frac{\pi}{3}-\frac{\pi}{4}  
$$  
Общий знаменатель 12:  
$$  
\frac{4\pi}{12}-\frac{3\pi}{12}=\frac{\pi}{12}  
$$

Итак, произведение $(1+i\sqrt3)(2-2i)=4\sqrt2\left(\cos\dfrac{\pi}{12}+i\sin\dfrac{\pi}{12}\right)$.

**Шаг 4. Применяем формулу Муавра, $n=8$.**

_Модуль:_  
$$  
r^8=(4\sqrt2)^8  
$$  
Перепишем основание через степени двойки: $4=2^2$, $\sqrt2=2^{1/2}$, значит $4\sqrt2=2^{2}\cdot2^{1/2}=2^{2+1/2}=2^{5/2}$.  
$$  
(4\sqrt2)^8=\left(2^{5/2}\right)^8=2^{\frac{5}{2}\cdot8}=2^{20}  
$$  
Считаем: $2^{10}=1024$, значит $2^{20}=(2^{10})^2=1024^2=1,048,576$.

_Угол:_  
$$  
8\varphi=8\cdot\frac{\pi}{12}=\frac{8\pi}{12}=\frac{2\pi}{3}  
$$  
(сократили $8/12=2/3$).

**Шаг 5. Угол $\dfrac{2\pi}{3}$ уже в табличном диапазоне $[0,2\pi)$ — приводить не нужно.**

**Шаг 6. Косинус и синус.**  
$$  
\cos\frac{2\pi}{3}=-\frac12,\qquad\sin\frac{2\pi}{3}=\frac{\sqrt3}{2}  
$$

**Шаг 7. Собираем ответ.**  
$$  
z=1,048,576\left(-\frac12+\frac{\sqrt3}{2}i\right)  
$$  
Раскрываем скобки (модуль умножаем отдельно на действительную и на мнимую часть):  
$$  
1,048,576\cdot\left(-\frac12\right)=-524288  
$$  
$$  
1,048,576\cdot\frac{\sqrt3}{2}=524288\sqrt3  
$$

$$  
z=-524288+524288\sqrt3,i  
$$

**Ответ:** $z=-524288+524288\sqrt3,i$

---

## 9. $z=\left(\dfrac{1+i\sqrt3}{2}\right)^{2026}+\left(\dfrac{1-i\sqrt3}{2}\right)^{2026}$

**Текст задания:** $z=\left(\dfrac{1+i\sqrt3}{2}\right)^{2026}+\left(\dfrac{1-i\sqrt3}{2}\right)^{2026}$

**Решения:**

Здесь сумма **двух** отдельных выражений в 2026-й степени. Каждое возводим в степень по формуле Муавра отдельно, а затем складываем результаты.

**Шаг 1. Тригонометрическая форма первого слагаемого $\dfrac{1+i\sqrt3}{2}$.**

$a=\dfrac12$, $b=\dfrac{\sqrt3}{2}$ — **I четверть**.  
$$  
r=\sqrt{\left(\frac12\right)^2+\left(\frac{\sqrt3}{2}\right)^2}=\sqrt{\frac14+\frac34}=\sqrt1=1  
$$  
$$  
\varphi=\arctan\frac{\sqrt3/2}{1/2}=\arctan\sqrt3=\frac{\pi}{3}  
$$

**Шаг 2. Тригонометрическая форма второго слагаемого $\dfrac{1-i\sqrt3}{2}$.**

Это число отличается от первого только знаком мнимой части ($a=\dfrac12$, $b=-\dfrac{\sqrt3}{2}$) — то есть это **сопряжённое** к первому число. У сопряжённого числа модуль тот же, а аргумент — противоположного знака:  
$$  
r=1,\qquad\varphi=-\frac{\pi}{3}  
$$

**Шаг 3. Возводим оба числа в степень 2026 по формуле Муавра.**

$$  
\left(\frac{1+i\sqrt3}{2}\right)^{2026}=1^{2026}\left(\cos\frac{2026\pi}{3}+i\sin\frac{2026\pi}{3}\right)=\cos\frac{2026\pi}{3}+i\sin\frac{2026\pi}{3}  
$$  
$$  
\left(\frac{1-i\sqrt3}{2}\right)^{2026}=1^{2026}\left(\cos\left(-\frac{2026\pi}{3}\right)+i\sin\left(-\frac{2026\pi}{3}\right)\right)=\cos\frac{2026\pi}{3}-i\sin\frac{2026\pi}{3}  
$$  
(во втором переходе использовано: $\cos(-x)=\cos x$ — чётность косинуса, $\sin(-x)=-\sin x$ — нечётность синуса).

**Шаг 4. Складываем.**

$$  
z=\left(\cos\frac{2026\pi}{3}+i\sin\frac{2026\pi}{3}\right)+\left(\cos\frac{2026\pi}{3}-i\sin\frac{2026\pi}{3}\right)  
$$  
Мнимые части взаимно уничтожаются ($+i\sin(\ldots)-i\sin(\ldots)=0$), действительные — удваиваются:  
$$  
z=2\cos\frac{2026\pi}{3}  
$$

Это ключевое наблюдение: **сумма сопряжённых комплексных чисел всегда действительна и равна удвоенной действительной части одного из них**.

**Шаг 5. Приводим угол $\dfrac{2026\pi}{3}$ к табличному значению.**

Угол вида $\dfrac{n\pi}{3}$ повторяется (даёт то же направление) при увеличении $n$ на $6$, потому что $\dfrac{(n+6)\pi}{3}=\dfrac{n\pi}{3}+2\pi$ — прибавление 6 к числителю добавляет ровно один полный оборот.

Значит, нужно найти остаток от деления $n=2026$ на $6$ — это и определит, какому "простому" углу из первого оборота эквивалентен наш огромный угол.

Делим 2026 на 6 столбиком: $6\cdot300=1800$, остаток $226$; $6\cdot37=222$, остаток $4$. Итого $6\cdot337=2022$, и $2026-2022=4$.  
$$  
2026=6\cdot337+4\quad\Rightarrow\quad 2026\equiv4\pmod6  
$$

Значит, $\dfrac{2026\pi}{3}$ эквивалентен $\dfrac{4\pi}{3}$ (заменили 2026 на его остаток 4 при делении на 6, потому что все "лишние" полные обороты не влияют на значение косинуса).

**Шаг 6. Косинус табличного угла.**

$\dfrac{4\pi}{3}$ — это $180°+60°=240°$, III четверть:  
$$  
\cos\frac{4\pi}{3}=-\frac12  
$$

**Шаг 7. Ответ.**  
$$  
z=2\cdot\left(-\frac12\right)=-1  
$$

**Ответ:** $z=-1$

---

## 10. $z=(1+i)^{4n}-(1-i)^{4n},\ n\in\mathbb N$

**Текст задания:** $z=(1+i)^{4n}-(1-i)^{4n}$, где $n\in\mathbb N$

**Решения:**

Здесь показатель степени — не конкретное число, а выражение с параметром $n$. Задача — доказать, что результат **не зависит от $n$** (или найти общую формулу через $n$), используя формулу Муавра в общем виде.

**Шаг 1. Тригонометрическая форма $1+i$.**

$a=1$, $b=1$ — **I четверть** (уже находили похожее в задаче 5).  
$$  
r=\sqrt2,\qquad\varphi=\frac{\pi}{4}  
$$

**Шаг 2. Тригонометрическая форма $1-i$.**

Это сопряжённое к $1+i$ число (та же действительная часть, противоположная мнимая):  
$$  
r=\sqrt2,\qquad\varphi=-\frac{\pi}{4}  
$$

**Шаг 3. Возводим $1+i$ в степень $4n$.**

По формуле Муавра, подставляя показатель $4n$ вместо конкретного числа:  
$$  
(1+i)^{4n}=(\sqrt2)^{4n}\left(\cos\left(4n\cdot\frac{\pi}{4}\right)+i\sin\left(4n\cdot\frac{\pi}{4}\right)\right)  
$$

Упрощаем модуль: $(\sqrt2)^{4n}=\left(2^{1/2}\right)^{4n}=2^{\frac12\cdot4n}=2^{2n}$.

Упрощаем угол: $4n\cdot\dfrac{\pi}{4}=n\pi$ (четвёрки сокращаются).

$$  
(1+i)^{4n}=2^{2n}\big(\cos(n\pi)+i\sin(n\pi)\big)  
$$

**Шаг 4. Ключевой факт: значения $\cos(n\pi)$ и $\sin(n\pi)$ для целого $n$.**

Угол $n\pi$ при целом $n$ — это либо $0$ (при чётном $n$), либо $\pi$ (при нечётном $n$), если смотреть только на направление (с точностью до полных оборотов). В обоих случаях:  
$$  
\sin(n\pi)=0\quad\text{для любого целого }n  
$$  
$$  
\cos(n\pi)=\begin{cases}1,&n\text{ чётное}\-1,&n\text{ нечётное}\end{cases}=(-1)^n  
$$  
(это стандартный табличный факт: точка при угле, кратном $\pi$, всегда лежит строго на действительной оси, поэтому мнимая часть — синус — обнуляется, а действительная часть — косинус — чередует знак $+1/-1$ в зависимости от чётности числа полуоборотов).

Подставляем:  
$$  
(1+i)^{4n}=2^{2n}\big((-1)^n+i\cdot0\big)=2^{2n}(-1)^n  
$$

**Шаг 5. Возводим $1-i$ в степень $4n$ — аналогично.**

$$  
(1-i)^{4n}=(\sqrt2)^{4n}\left(\cos\left(4n\cdot\left(-\frac{\pi}{4}\right)\right)+i\sin\left(4n\cdot\left(-\frac{\pi}{4}\right)\right)\right)=2^{2n}\big(\cos(-n\pi)+i\sin(-n\pi)\big)  
$$

Используем чётность косинуса и нечётность синуса:  
$$  
\cos(-n\pi)=\cos(n\pi)=(-1)^n,\qquad\sin(-n\pi)=-\sin(n\pi)=-0=0  
$$

$$  
(1-i)^{4n}=2^{2n}\big((-1)^n+i\cdot0\big)=2^{2n}(-1)^n  
$$

**Шаг 6. Замечаем, что оба результата совпали.**

Несмотря на то, что мы возводили в степень разные числа ($1+i$ и $1-i$), после подстановки $4n$ в качестве показателя оба выражения дали **одну и ту же величину** $2^{2n}(-1)^n$. Это не случайность: угол у $1-i$ был противоположным углу $1+i$ ($-\pi/4$ вместо $\pi/4$), но при умножении на $4n$ и последующем взятии косинуса/синуса от кратного $\pi$ это различие "стирается" — что для $n\pi$, что для $-n\pi$ косинус и синус дают одинаковый результат ($\cos$ — чётная функция, а $\sin(n\pi)=0$ обнуляет саму зависимость от знака).

**Шаг 7. Вычитаем.**

$$  
z=(1+i)^{4n}-(1-i)^{4n}=2^{2n}(-1)^n-2^{2n}(-1)^n=0  
$$

**Шаг 8. Вывод.**

Так как это равенство не зависит от конкретного значения $n$ (мы использовали только то, что $n$ — целое число, ни разу не подставляя конкретное числовое значение), результат $z=0$ выполняется **для любого** $n\in\mathbb N$.

**Ответ:** $z=0$ при любом $n\in\mathbb N$

---

> [!warning] Заметки по фактчекингу  
> Ошибок в условиях задач не обнаружено — все выражения корректны и однозначно вычислимы по формуле Муавра. Задача 10 не является опечаткой, несмотря на присутствие параметра $n$: тождественное равенство $z=0$ доказано в общем виде (шаги 3–7) и не зависит от конкретного значения $n$.